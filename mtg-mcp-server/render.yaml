services:
  - type: web
    name: mtg-mcp-server
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: node dist/http-server.js
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
